import { FaceRecognitionUtil } from './face-recognition.util';

describe('FaceRecognitionUtil', () => {
  it('should be defined', () => {
    expect(new FaceRecognitionUtil()).toBeDefined();
  });
});
